# GitHub OAuth Configuration
# Create a GitHub OAuth App at: https://github.com/settings/applications/new
# For production, create a separate OAuth app with production callback URL
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Security Configuration
# Generate a secure random string for JWT signing (use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your_secure_jwt_secret_here

# Environment Configuration
NODE_ENV=development
PORT=3000

# Frontend URLs (for CORS configuration)
FRONTEND_URL=http://localhost:5173

# Production Configuration (uncomment for production)
# NODE_ENV=production
# PORT=443
# FRONTEND_URL=https://mdsg.daza.ar
# JWT_SECRET=your_production_jwt_secret_here

# OAuth Callback URLs:
# Development: http://localhost:3000/auth/github/callback
# Production: https://mdsg-api.daza.ar/auth/github/callback

# Security Notes:
# - Never commit actual secrets to version control
# - Use different OAuth apps for development and production
# - Rotate JWT secrets regularly in production
# - Enable HTTPS in production
# - Use secure headers and rate limiting
